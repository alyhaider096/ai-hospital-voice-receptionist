# QA Report - 2026-07-10

## Scope

Detailed backend QA pass for the AI Hospital Voice Receptionist foundation.

Tested areas:

```txt
FastAPI app boot
Vapi bearer authentication
Admin authentication
Doctor matching
Emergency safety routing
Availability generation
Schedule exceptions
Appointment booking
Idempotent booking retry
Double-booking rejection
PII encryption and redaction
End-of-call logging
Admin appointments/doctors/call logs
Alembic migration upgrade/downgrade
Live localhost API smoke flow
```

## Automated Test Result

Command:

```bash
cd backend
.\.venv\Scripts\python.exe -m pytest
```

Result:

```txt
24 passed, 1 warning
```

The warning is from FastAPI/Starlette's TestClient dependency path:

```txt
StarletteDeprecationWarning: Using httpx with starlette.testclient is deprecated
```

This does not currently fail behavior. Track it when upgrading FastAPI/Starlette
or if the test client API changes.

## Migration Result

Command:

```bash
set DATABASE_URL=sqlite:///./migration_test.sqlite
alembic upgrade head
alembic downgrade base
alembic upgrade head
```

Result:

```txt
Passed
```

The throwaway migration database was removed after the check.

## Live API Smoke Result

Server:

```txt
http://127.0.0.1:8001
```

Result:

```txt
login: ok
matched_doctor: Dr. Ayesha Khan
availability_date: 2026-07-11
booked_ref: APT-10001
double_booking: rejected
call_log_privacy: summary/transcript hidden behind flags
```

## Bugs / Gaps Found And Fixed

### Missing planned endpoints

The original backend slice had documented but missing routes:

```txt
POST /auth/login
POST /auth/logout
GET  /admin/call-logs
```

Fixed by adding:

```txt
backend/app/api/routes/auth.py
backend/app/api/routes/admin.py call-log list endpoint
backend/app/schemas/auth.py
backend/app/schemas/admin.py CallLogListItem
```

### Direct seed script execution

The seed script failed when run directly from PowerShell because `app` was not
on `sys.path`.

Fixed in:

```txt
backend/scripts/seed.py
```

### Test coverage gaps

Added coverage for:

```txt
wrong Vapi token
unknown symptoms fallback
past date rejection
unknown/inactive doctor rejection
blocked schedule exceptions
extra schedule slots
cancelled appointment reopening slot
PII encryption and patient reuse by phone hash
call-log idempotent update
admin login/logout
admin doctors list
admin appointment status update audit log
admin invalid status rejection
admin call-log privacy
```

## Remaining QA Before Vapi Web Call

```txt
Run backend against local PostgreSQL, not only SQLite
Expose backend with ngrok/cloudflared
Configure Vapi Web Call tools with bearer credential
Perform full voice call booking
Verify appointment via admin endpoint/dashboard
Confirm assistant wording does not diagnose
Test emergency phrase during actual voice call
```

## Remaining QA Before Official Number

```txt
Rotate VAPI_TOOL_SECRET and ADMIN_API_TOKEN
Use production HTTPS URL instead of tunnel
Enable PostgreSQL backups
Lock CORS to dashboard origin
Replace temporary admin bearer token with HttpOnly session auth
Approve hospital greeting and consent wording
Decide transcript/recording retention policy
Run a 20-call manual test script with fake data
```

